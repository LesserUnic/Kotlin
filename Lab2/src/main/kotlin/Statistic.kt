import kotlin.collections.Map

class Statistic {
    fun statistic(list:MutableMap<String, List<Info>>){
        println("In work ${list.size} strings")

        //println(list.first.city+" "+list.first.street+" "+list.first.house+" "+list.first.floor)
    }
}